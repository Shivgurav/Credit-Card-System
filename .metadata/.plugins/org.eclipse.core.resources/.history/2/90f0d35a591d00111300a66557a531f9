package com.payPanda.repository;

import com.payPanda.entity.CreditCardEntity;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface CreditCardRepository extends JpaRepository<CreditCardEntity, Long> {

    List<CreditCardEntity> findByCustomerId(Long customerId);

    // Used by transaction-service via GET /cards/{cardNumber}
    // Already handled by findById(cardNumber) from JpaRepository
}