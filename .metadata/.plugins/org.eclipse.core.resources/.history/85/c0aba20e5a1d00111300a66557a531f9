package com.payPanda.service;

import com.payPanda.dto.*;

import java.math.BigDecimal;
import java.util.List;

public interface CardService {

    // ── Application flow ──────────────────────────────────────────────────────
    CardApplicationResponseDTO submitApplication(CardApplicationRequestDTO request);

    List<CardApplicationResponseDTO> getPendingApplications();

    List<CardApplicationResponseDTO> getAllApplications();

    CardApplicationResponseDTO reviewApplication(Long applicationId, ApplicationReviewDTO review);

    // ── Card operations ───────────────────────────────────────────────────────
    CreditCardResponseDTO getCardByNumber(Long cardNumber);

    List<CreditCardResponseDTO> getCardsByCustomer(Long customerId);

    CreditCardResponseDTO updateCardStatus(CardStatusUpdateDTO request);

    // ── Called by transaction-service ─────────────────────────────────────────
    void deductAvailableLimit(Long cardNumber, BigDecimal amount);
}